# Plan: Revisión y Actualización de Documentación de Configuración (.kilo/ y AGENTS.md)

## Resumen de Hallazgos

La auditoría reveló **3 niveles de problemas**: críticos (la documentación dice algo diferente al código real), moderados (información incompleta) y la mejora pendiente (AGENTS.md sin mapeo de `.kilo/`).

---

## Cambio 1: AGENTS.md — Agregar mapeo completo de `.kilo/` (PENDIENTE de conversación anterior)

**Problema**: `AGENTS.md` solo referencia `Memory Bank` pero no menciona `agent/`, `command/` ni `kilo.json`. Otras herramientas de IA (Gemini, Cursor, Qwen) no pueden descubrir estos archivos.

**Archivo**: `AGENTS.md`

**Cambio**: Reemplazar la sección `## Memory Bank` con `## AI Agent Configuration (.kilo/)` que mapea:
- `kilo.json` (root config)
- `.kilo/agent/` (code.md, architect.md)
- `.kilo/command/` (test.md, lint.md, typecheck.md, migrate.md)
- `.kilo/rules/memory-bank/` (6 archivos con descripciones expandidas)

---

## Cambio 2: systemPatterns.md — Corregir clase Settings (CRÍTICO)

**Problema**: La documentación muestra una clase `Settings` que difiere significativamente del código real en `app/core/config.py`:

| Aspecto | Documentado | Real |
|---------|-------------|------|
| `MAX_LOGIN_ATTEMPTS` | Field name | Se llama `SECURITY_LOGIN_MAX_ATTEMPTS` |
| `LOCKOUT_DURATION_MINUTES` | Field name | Se llama `SECURITY_LOCKOUT_MINUTES` |
| `LOCKOUT_DURATION_MINUTES` default | `= 30` | `= 15` |
| `ACCESS_TOKEN_EXPIRE_MINUTES` default | `= 15` | Sin default (requerido) |
| `REFRESH_TOKEN_EXPIRE_DAYS` default | `= 7` | Sin default (requerido) |
| 13+ campos adicionales | No documentados | Existen (ALGORITHM, TIME_ZONE, PROJECT_NAME, VERSION, PORT, ENVIRONMENT, BACKEND_CORS_ORIGINS, ENABLE_ACCESS_AUDIT, ENABLE_DATA_AUDIT, ENABLE_ACCESS_LOGS, ACCESS_LOGS_ONLY_ERRORS, AUDIT_EXCLUDED_PATHS, AUDIT_LOG_EXCLUDE_STATUS_CODES, AUDIT_LOG_INCLUDED_METHODS, PROJECT_ROOT) |

**Archivo**: `.kilo/rules/memory-bank/systemPatterns.md` (líneas 148-156)

**Cambio**: Actualizar el bloque de código de `Settings` para reflejar la clase real.

---

## Cambio 3: systemPatterns.md — Corregir clases de excepciones (CRÍTICO)

**Problema**: La documentación muestra excepciones que heredan de `HTTPException` con defaults, pero el código real usa una jerarquía diferente:

| Aspecto | Documentado | Real |
|---------|-------------|------|
| Clase base | `HTTPException` | `CustomException(Exception)` |
| `NotFoundException` default | `"Resource not found"` | Sin default (requerido) |
| `ForbiddenException` default | `"Access denied"` | Sin default (requerido) |
| Firma del constructor | `(self, detail: str)` | `(self, detail: Any, headers: Optional[dict] = None)` |
| Status code | Embebido en cada clase | Manejado externamente en `handlers.py` |
| `BadRequestException` | No documentada | Existe |
| `UnauthorizedException` | No documentada | Existe |
| `InternalServerErrorException` | No documentada | Existe |
| `CustomException` base | No documentada | Existe |

**Archivo**: `.kilo/rules/memory-bank/systemPatterns.md` (líneas 129-137)

**Cambio**: Actualizar el bloque de código de excepciones para reflejar la jerarquía real, incluyendo `CustomException` y las 5 excepciones derivadas.

---

## Cambio 4: techContext.md — Corregir tabla de variables de entorno (CRÍTICO)

**Problema**: La tabla de variables de entorno (líneas 59-67) tiene los mismos errores que systemPatterns.md: nombres de campos incorrectos y defaults erróneos.

**Archivo**: `.kilo/rules/memory-bank/techContext.md` (líneas 59-67)

**Cambio**: Actualizar la tabla con los nombres correctos (`SECURITY_LOGIN_MAX_ATTEMPTS`, `SECURITY_LOCKOUT_MINUTES`), los defaults correctos, y agregar las variables faltantes (ALGORITHM, TIME_ZONE, etc.).

---

## Cambio 5: systemPatterns.md — Documentar archivos adicionales en docs/ y scripts/ (MODERADO)

**Problema**: Solo 4 de 19 archivos en `docs/` están documentados. Solo 1 de 3 scripts está documentado.

**Documentación faltante en `docs/`**:
- ASSETS_MODULE_GUIDE.md
- AUTH_ANALYSIS.md
- CODE_STANDARDS_REVIEW.md
- CORE_MODULE_GUIDE.md
- DESIGN_PATTERNS_GUIDE.md
- DEVELOPER_GUIDE.md
- EXCEPTION_HANDLING_GUIDE.md
- FRONTEND_AUTH_GUIDE.md
- OBSERVABILITY_GUIDE.md
- PROJECT_ANALYSIS_REVIEW.md
- PROJECT_ANALYSIS.md
- QUALITY_GUIDE.md
- QUERY_SYSTEM_GUIDE.md
- SOLID_GUIDE.md
- TESTING_GUIDE.md

**Scripts faltantes**:
- scripts/demo_audit.py
- scripts/reset_db_schema.py

**Archivo**: `.kilo/rules/memory-bank/systemPatterns.md` (líneas 206-209) y `.kilo/rules/memory-bank/techContext.md` (línea 101)

**Cambio**: Agregar la lista completa de documentación y scripts.

---

## Cambio 6: AGENTS.md — Agregar referencia a excepciones adicionales (MENOR)

**Problema**: `AGENTS.md` línea 36 solo menciona `NotFoundException` y `ForbiddenException`, pero el proyecto también tiene `BadRequestException`, `UnauthorizedException` e `InternalServerErrorException`.

**Archivo**: `AGENTS.md` (línea 36)

**Cambio**: Ampliar la lista de excepciones custom.

---

## Cambio 7: code.md y architect.md — Consistencia con excepciones (MENOR)

**Problema**: `.kilo/agent/code.md` línea 40 dice "Use NotFoundException/ForbiddenException for error handling" pero no menciona las otras 3 excepciones.

**Archivo**: `.kilo/agent/code.md` (línea 40)

**Cambio**: Ampliar la referencia a todas las excepciones custom.

---

## Detalle de Ejecución

### Paso 1: AGENTS.md — Reescritura completa

Reemplazar todo el contenido de `AGENTS.md` con:

```markdown
# Uyuni Backend - Project Instructions

## Project Overview
Uyuni Backend is a Python/FastAPI enterprise backend with JWT auth, RBAC, audit logging, and modular domain architecture (assets, core, tasks).

## Tech Stack
- **Framework**: FastAPI (Python 3.10+) with Pydantic v2
- **ORM**: SQLModel (SQLAlchemy + Pydantic), async
- **Database**: PostgreSQL (prod) / SQLite (dev/test)
- **Auth**: JWT with access/refresh token rotation, bcrypt
- **Migrations**: Alembic
- **Logging**: structlog
- **Quality**: Ruff (lint), Mypy (types), pytest
- **Deployment**: Vercel Serverless

## Architecture
```
Routers (API) → Services (logic) → Repositories (data) → Models (entities)
```

### Module Structure (DDD-lite)
Each domain module contains: `routers.py`, `service.py`, `repository.py`, `models.py`, `schemas.py`
- **Flat Hierarchy**: Base domains contain sibling submodules
- **Nested Routing**: Root module routers act as unified aggregators

## Key Patterns
- **Repository Pattern**: Generic `BaseRepository[T]` with CRUD interface
- **Dependency Injection**: FastAPI `Depends()` throughout
- **RBAC**: User → Role → RoleModule → Module + Permissions (CRUD)
- **Audit**: Middleware captures request context + SQLAlchemy hooks for CDC

## Code Conventions
- Strict Clean Code principles (SOLID, DRY, KISS)
- Complete type hints required
- Use structlog for all logging (never print())
- Custom exceptions: `NotFoundException`, `ForbiddenException`, `BadRequestException`, `UnauthorizedException`, `InternalServerErrorException` (all extend `CustomException`)
- All models must be imported in `alembic/env.py`
- Use Alembic migrations (never `create_all()` in production)
- In-Memory SQLite for tests

## Commands
- `/test` - Run tests with pytest
- `/lint` - Run Ruff linting and formatting
- `/typecheck` - Run Mypy type checking
- `/migrate` - Run Alembic database migrations

## AI Agent Configuration (`.kilo/`)

This project uses a `.kilo/` configuration directory for AI agent tooling.
Other AI tools (Cursor, Gemini, Qwen, Kimi, Cline, etc.) can reference these files for full project context.

### Root Config
- `kilo.json` - Points to all instruction files and sets `code` as default agent

### Agents (`.kilo/agent/`)
- `code.md` - **Primary agent**: Senior Backend Developer with full edit/bash permissions. Handles coding, testing, migrations, and linting.
- `architect.md` - **Sub-agent**: Architecture and database design specialist. Read-heavy with restricted edits. Invoked internally by `code` for strategic decisions.

### Commands (`.kilo/command/`)
- `test.md` - Defines `/test` command: runs `pytest $ARGUMENTS` with optional file/directory targeting
- `lint.md` - Defines `/lint` command: runs `ruff check --fix` + `ruff format` on `app/` and `tests/`
- `typecheck.md` - Defines `/typecheck` command: runs `mypy app/ --ignore-missing-imports`
- `migrate.md` - Defines `/migrate` command: runs `alembic upgrade head` or `alembic revision --autogenerate` with a message

### Memory Bank (`.kilo/rules/memory-bank/`)
Detailed project context maintained across sessions:
- `projectbrief.md` - Project objectives, features, structure, target users, and success metrics
- `productContext.md` - User personas (Admin, Asset Manager, Staff Manager, Auditor), workflows, and technical constraints
- `systemPatterns.md` - Architecture layers, design patterns (Repository, DI, RBAC, Audit), data models, error handling, and testing strategy
- `techContext.md` - Tech stack details, dev setup, environment variables, project structure, API endpoints, database schema, and deployment
- `progress.md` - Completed milestones (5 phases), in-progress items, pending features, known issues, and technical debt
- `developerPersona.md` - Developer role definition: Senior Backend Architect with 10 core competencies and response style guidelines
```

---

### Paso 2: systemPatterns.md — Corregir Settings (líneas 148-156)

Reemplazar el bloque de código de Settings:

```python
class Settings(BaseSettings):
    DATABASE_URL: str
    SECRET_KEY: str
    ACCESS_TOKEN_EXPIRE_MINUTES: int = 15
    REFRESH_TOKEN_EXPIRE_DAYS: int = 7
    MAX_LOGIN_ATTEMPTS: int = 5
    LOCKOUT_DURATION_MINUTES: int = 30
```

Con:

```python
class Settings(BaseSettings):
    # App
    PROJECT_NAME: str = "Uyuni-BackEnd"
    VERSION: str = "v1"
    PORT: int = 8000
    ENVIRONMENT: str = "local"

    # CORS
    BACKEND_CORS_ORIGINS: list[AnyHttpUrl] = []

    # Database
    DATABASE_URL: str

    # Auth
    SECRET_KEY: str
    ALGORITHM: str
    ACCESS_TOKEN_EXPIRE_MINUTES: int
    REFRESH_TOKEN_EXPIRE_DAYS: int

    # Security Lockout
    SECURITY_LOGIN_MAX_ATTEMPTS: int = 5
    SECURITY_LOCKOUT_MINUTES: int = 15

    # Utils
    TIME_ZONE: int
    PROJECT_ROOT: str = ...

    # Audit
    ENABLE_ACCESS_AUDIT: bool = True
    ENABLE_DATA_AUDIT: bool = True

    # Log Controls
    ENABLE_ACCESS_LOGS: bool = True
    ACCESS_LOGS_ONLY_ERRORS: bool = False
    AUDIT_EXCLUDED_PATHS: list[str] = [...]
    AUDIT_LOG_EXCLUDE_STATUS_CODES: list[int] = [404]
    AUDIT_LOG_INCLUDED_METHODS: list[str] = ["GET", "POST", "PUT", "PATCH", "DELETE"]
```

---

### Paso 3: systemPatterns.md — Corregir excepciones (líneas 126-143)

Reemplazar el bloque `### Custom Exceptions` + `### Global Handlers`:

```python
class NotFoundException(HTTPException):
    def __init__(self, detail: str = "Resource not found"):
        super().__init__(status_code=404, detail=detail)

class ForbiddenException(HTTPException):
    def __init__(self, detail: str = "Access denied"):
        super().__init__(status_code=403, detail=detail)
```

Y la sección de handlers, con:

```python
class CustomException(Exception):
    def __init__(self, detail: Any, headers: Optional[dict[str, str]] = None):
        self.detail = detail
        self.headers = headers

class NotFoundException(CustomException): ...
class BadRequestException(CustomException): ...
class UnauthorizedException(CustomException): ...
class ForbiddenException(CustomException): ...
class InternalServerErrorException(CustomException): ...
```

### Global Handlers
Registered in `app/core/handlers.py` — each handler maps an exception to its HTTP status code:
- `NotFoundException` → 404 response
- `BadRequestException` → 400 response
- `UnauthorizedException` → 401 response
- `ForbiddenException` → 403 response
- `InternalServerErrorException` → 500 response (with logging)

---

### Paso 4: techContext.md — Corregir tabla de variables de entorno (líneas 59-67)

Reemplazar la tabla:

| Variable | Description | Default |
|----------|-------------|---------|
| `DATABASE_URL` | PostgreSQL connection string | Required |
| `SECRET_KEY` | JWT signing key | Required |
| `ACCESS_TOKEN_EXPIRE_MINUTES` | Access token lifetime | 15 |
| `REFRESH_TOKEN_EXPIRE_DAYS` | Refresh token lifetime | 7 |
| `MAX_LOGIN_ATTEMPTS` | Failed attempts before lockout | 5 |
| `LOCKOUT_DURATION_MINUTES` | Account lockout duration | 30 |

Con:

| Variable | Description | Default |
|----------|-------------|---------|
| `PROJECT_NAME` | Application name | `"Uyuni-BackEnd"` |
| `VERSION` | API version | `"v1"` |
| `PORT` | Server port | `8000` |
| `ENVIRONMENT` | Runtime environment | `"local"` |
| `BACKEND_CORS_ORIGINS` | Allowed CORS origins | `[]` |
| `DATABASE_URL` | PostgreSQL connection string | **Required** |
| `SECRET_KEY` | JWT signing key | **Required** |
| `ALGORITHM` | JWT algorithm | **Required** |
| `ACCESS_TOKEN_EXPIRE_MINUTES` | Access token lifetime (minutes) | **Required** |
| `REFRESH_TOKEN_EXPIRE_DAYS` | Refresh token lifetime (days) | **Required** |
| `SECURITY_LOGIN_MAX_ATTEMPTS` | Failed attempts before lockout | `5` |
| `SECURITY_LOCKOUT_MINUTES` | Account lockout duration (minutes) | `15` |
| `TIME_ZONE` | UTC offset | **Required** |
| `ENABLE_ACCESS_AUDIT` | Enable access audit logging | `True` |
| `ENABLE_DATA_AUDIT` | Enable CDC audit logging | `True` |
| `ENABLE_ACCESS_LOGS` | Master switch for access logging | `True` |
| `ACCESS_LOGS_ONLY_ERRORS` | Only log 4xx/5xx responses | `False` |

---

### Paso 5: systemPatterns.md — Agregar docs/ y scripts/ faltantes (líneas 200-209)

Reemplazar la sección `### Reference Documentation` con la lista completa de 19 archivos en docs/.

Agregar `scripts/demo_audit.py` y `scripts/reset_db_schema.py` a la sección de Deployment o scripts.

---

### Paso 6: techContext.md — Agregar scripts faltantes (línea 101)

Agregar `demo_audit.py` y `reset_db_schema.py` al árbol de directorios bajo `scripts/`.

---

### Paso 7: code.md — Corregir referencia a excepciones (línea 40)

Reemplazar:
```
4. Use NotFoundException/ForbiddenException for error handling
```
Con:
```
4. Use CustomException hierarchy: NotFoundException, BadRequestException, UnauthorizedException, ForbiddenException, InternalServerErrorException
```
